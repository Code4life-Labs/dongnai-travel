import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  buttonsContainer: {
    position: "absolute",
    flexDirection: "row",
    bottom: 18,
    right: 18,
  },
});
